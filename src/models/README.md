# Models

In the MVC design pattern, Models are responsible for representing and managing application data and state. Logic in this section includes storing user-defined settings, maintaining the song queues, and persisting data (e.g., to the file system). Models should not depend on Discord-specific APIs.