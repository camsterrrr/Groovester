# Controllers

In the MVC design pattern, Controllers act as the intermediary between Models and Views. In this application, Controllers may expose getters/setters or can coordinate more complex logic like verifying file system state.